# Benchmarks

This folder stores the benchmark manifests and summary numbers used in the wrapper repo.

Files:
- `combined_benchmark.csv` - 63-clip benchmark manifest
- `pandeya_bench.csv` - 50-clip unseen Pandeya benchmark manifest
- `combined63_all_models.json` - summary comparison for v1, v2L, v2S, cat-alan
